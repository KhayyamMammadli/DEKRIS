import { useState } from 'react';
import { Card } from '../components/ui/Card';
import { store, useStore } from '../store/appStore';
export default function Settings(){ const s=useStore(); const [name,setName]=useState(s.user?.name||''); const [role,setRole]=useState(s.user?.role||''); const [email,setEmail]=useState(s.user?.email||''); return <><div className="page-title"><h2>Parametrlər</h2><p>Profil, bildiriş və sistem seçimləri</p></div><div className="grid-3"><Card title="Profil məlumatları"><label>Ad, soyad</label><input value={name} onChange={e=>setName(e.target.value)}/><label>Vəzifə</label><input value={role} onChange={e=>setRole(e.target.value)}/><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)}/><button className="primary" onClick={()=>{store.updateProfile({name,role,email});alert('Profil saxlandı')}}>Yadda saxla</button></Card><Card title="Bildirişlər"><label><input type="checkbox" defaultChecked/> Yeni müraciətlər barədə xəbər ver</label><label><input type="checkbox" defaultChecked/> Son tarix yaxınlaşanda xəbərdar et</label><label><input type="checkbox"/> Email bildirişləri aktiv et</label></Card><Card title="Təhlükəsizlik"><label>Yeni şifrə</label><input type="password"/><label>Təkrar şifrə</label><input type="password"/><button onClick={()=>alert('Şifrə dəyişdirildi')}>Şifrəni dəyiş</button><button className="danger" onClick={()=>{store.reset();alert('Demo data sıfırlandı')}}>Demo datanı sıfırla</button></Card></div></>}
