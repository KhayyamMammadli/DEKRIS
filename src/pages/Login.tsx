import { FormEvent, useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { store } from '../store/appStore';
export default function Login(){ const nav=useNavigate(); const [email,setEmail]=useState('rashad.aliyev@dekris.gov.az'); const [password,setPassword]=useState('123456'); const [err,setErr]=useState(''); function submit(e:FormEvent){e.preventDefault(); try{store.login(email,password); nav('/dashboard')}catch(x:any){setErr(x.message)}} return <div className="login-page"><div className="login-card"><div className="login-brand"><div className="logo big">✦</div><h1>DEKRIS</h1><p>Daşınmaz Əmlakın Dövlət Kadastrı və Reyestri İnformasiya Sistemi</p></div><form onSubmit={submit}><label>Email</label><input value={email} onChange={e=>setEmail(e.target.value)}/><label>Şifrə</label><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/>{err&&<div className="error">{err}</div>}<button className="primary full">Daxil ol</button><button type="button" className="ghost full" onClick={()=>alert('Demo üçün email: rashad.aliyev@dekris.gov.az / şifrə: 123456')}>Şifrəni unutdum</button></form></div></div>}
